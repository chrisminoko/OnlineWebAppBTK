@model IEnumerable<OnlineWebApp.Models.AppModels.Discount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Items.Item_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Discount_Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Items.Item_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Discount_Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Discount_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Discount_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Discount_Id })
        </td>
    </tr>
}

</table>
